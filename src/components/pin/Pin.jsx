import React from "react";

export default class Pin extends React.Component {
    constructor(center, option) {
        super(center, option);
        this.center = center
        this.option = option
    }
}